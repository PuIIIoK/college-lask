import ScheduleChanges from '../../components/ScheduleChanges/ScheduleChanges';

export const metadata = {
  title: 'Изменения в расписании — ГБПОО ЛАСК',
};

export default function ScheduleChangesPage() {
  return <ScheduleChanges />;
}
